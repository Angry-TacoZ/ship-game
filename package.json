{
  "name": "ship-game",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "node server/benchmark.mjs",
    "test": "node --test tests/*.test.mjs",
    "package:static": "node scripts/package-static.mjs",
    "verify:controls": "node scripts/duel-controls.mjs",
    "verify": "npm run test && node scripts/verify-project.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
